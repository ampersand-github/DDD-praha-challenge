import { TeamName } from './teamName';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function duplicateTeamDomainService(name: TeamName): boolean {
  /*
  todo
   DBからチーム名の一覧を取得する
   引数のチーム名が存在するならture、存在しないならfalse
   */
  return true;
}
